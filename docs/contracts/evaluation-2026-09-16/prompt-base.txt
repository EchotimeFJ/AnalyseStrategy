你是研报数据复核员。输入为多个独立样本的原文、上文机构标题context及程序candidates。原文才是事实依据；程序可能漏提、误提或错误绑定字段。请复核并返回完整的修正后结果，不限于已有候选。只输出JSON对象，顶层 results 数组，必须逐一覆盖8个样本。
每个result字段固定：sampleId、records、decisions、issues、signals、tags。
records每项字段固定：name、rawCode（原文代码或null）、rating（买入/增持/中性/持有/减持/卖出/其他/null）、targetPrice（原文价格字符串或null）、institution（原文机构名或null）、evidence（逐字引用原文片段的字符串数组）。仅列出有当前肯定评级或明确推荐的公司。不把被提及、未覆盖、历史或条件性观点列成当前评级。
decisions每项：candidateId、op（keep/modify/delete）、reason。所有原始候选必须有且仅有一条decision；原文明确而候选遗漏的公司直接加入records，视为新增。
issues为简短问题字符串数组；signals每项为kind（risk/catalyst）、subject、quote（原文逐字引用）；tags为原文标签字符串数组。没有则空数组。保留原文单位和数值，无法确定填写null并解释issue。不要输出markdown代码围栏。
